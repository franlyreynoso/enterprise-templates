# Enterprise UI Template - Staging Environment# Enterprise UI Template - Staging Environment

# Exact backend template variable naming for perfect integration# Matches backend template variable naming conventions



# Environment# Application Configuration

ENV=stagingASPNETCORE_ENVIRONMENT=Staging

ASPNETCORE_ENVIRONMENT=StagingUI_HTTP_PORT=7001

UI_HTTPS_PORT=7101

# Database Configuration (matches backend exactly - shared service)CERTIFICATE_PASSWORD=${CERT_PASSWORD_STAGING}

DB_USER=app

DB_PASS=app_staging_secure# Database Configuration (matches backend exactly)

POSTGRES_DB=enterprisetemplate_stagingDB_HOST=postgres-staging.enterprise.local

POSTGRES_PORT=5433DB_PORT=5432

DB_NAME=enterpriseui_staging

# Application Ports (UI uses different ports to avoid API conflicts)DB_USER=enterpriseui_staging

UI_HTTP_PORT=3001DB_PASS=${DB_PASSWORD_STAGING}

UI_HTTPS_PORT=3101

# Seq Centralized Logging (matches backend)

# Observability Configuration (matches backend exactly - shared services)SEQ_PORT=5341

OTEL_GRPC_PORT=4318SEQ_INGESTION_PORT=5341

OTEL_HTTP_PORT=4319SEQ_PASSWORD_HASH=${SEQ_PASSWORD_HASH_STAGING}

JAEGER_UI_PORT=16687

JAEGER_OTLP_GRPC_PORT=14318# OpenTelemetry & Jaeger Configuration (matches backend)

SEQ_PORT=5342OTEL_GRPC_PORT=4317

OTEL_HTTP_PORT=4318

# Message Queue Configuration (matches backend exactly - shared service)OTEL_HEALTH_PORT=13133

RABBITMQ_USER=appJAEGER_UI_PORT=16686

RABBITMQ_PASS=app_staging_secureJAEGER_GRPC_PORT=14250

RABBITMQ_PORT=5673JAEGER_THRIFT_PORT=14268

RABBITMQ_MANAGEMENT_PORT=15673JAEGER_ZIPKIN_PORT=9411



# Cache Configuration (matches backend exactly - shared service)# Docker Compose Profile

REDIS_PORT=6380COMPOSE_PROFILES=staging

# Development Tools (matches backend exactly)
PGADMIN_PORT=5051
MAILHOG_SMTP_PORT=1026
MAILHOG_UI_PORT=8026

# Connection Strings (for UI application to use shared services)
CONNECTIONSTRING_DEFAULT=Host=localhost;Port=5433;Database=enterprisetemplate_staging;Username=app;Password=app_staging_secure
CONNECTIONSTRING_REDIS=localhost:6380
CONNECTIONSTRING_RABBITMQ=amqp://app:app_staging_secure@localhost:5673

# OpenTelemetry Configuration (matches backend exactly)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_SERVICE_NAME=enterprise-ui
OTEL_RESOURCE_ATTRIBUTES=service.namespace=enterprise,deployment.environment=staging

# Security Configuration (Staging)
ENABLE_HTTPS=true
AUTH_MODE=JWT
AUTH_AUTHORITY=https://staging-auth.example.com
AUTH_AUDIENCE=enterprise-ui-staging